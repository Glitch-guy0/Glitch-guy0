# Glitch-guy0 — copy to `.env` and fill. Server-side only (never committed).

# Contact flow → EmailOctopus (free tier). See tech-stack.md.
EMAIL_OCTOPUS_API_KEY=
EMAIL_OCTOPUS_LIST_ID=

# Public contact email — header/footer mailto: + no-JS form fallback.
CONTACT_EMAIL=prajwalm9845@gmail.com

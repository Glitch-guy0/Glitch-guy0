---
scope: v1 freelance harness-engineer portfolio (Next.js App Router on Vercel)
purpose: build substrate for independent builders implementing the PRD
altitude: feature
updated: 2026-08-06T19:25
---

- (constraint) stack locked by tech-stack.md: Next.js App Router + Tailwind on Vercel, GSAP+Lenis motion, EmailOctopus contact delivery, Vercel Web Analytics
- (constraint) strictly monochrome palette, no chromatic color anywhere in v1 (DESIGN.md)
- (constraint) WCAG 2.1 AA; glitch bursts 100-400ms, snap-back, never body text/forms/page; prefers-reduced-motion disables outright (EXPERIENCE.md)
- (constraint) PRD decisions bind: 7 sections, 3 no-price offers, 3 featured+showcase projects, 6-8 pills, 3-field contact, EmailOctopus, consent banner, Lighthouse>=90 mobile
- (decision) AD-1 design tokens single source of truth in Tailwind v4 @theme; zero raw design literals in components -- prevents chromatic leak and value drift
- (decision) AD-2 motion centralized in src/lib/motion (GSAP 3 + Lenis 1 on one GSAP ticker); one global reduced-motion gate via gsap.matchMedia; no other animation lib -- prevents scattered tweens breaking WCAG
- (decision) AD-3 contact flow is a closed loop: client -> POST /api/contact -> zod+honeypot -> EmailOctopus -> explicit success/error; route is only secret reader; no silent 200 -- prevents lost submissions and key leaks
- (decision) AD-4 all copy single-sourced in src/content/ from resume.md + PRD approved sets; resume.pdf generated from same content at build -- prevents copy drift
- (decision) AD-5 server-first island architecture: sections server components, client islands only MotionProvider/Header/ContactForm/CookieBanner; no client data-fetching -- prevents whole-page client bundle, holds Lighthouse
- (decision) AD-6 analytics consent-gated: Vercel Web Analytics inits only after Accept, persisted; single banner surface; none outside production -- prevents pre-consent tracking
- (decision) AD-7 static-first, no DB/auth/server state; only transient client state (form+consent); outbound external calls only from /api/contact -- keeps cost/privacy guardrails
- (version) verified on web: Next.js 15/16 App Router current (create-next-app default), Tailwind v4 (CSS-first @theme, no config file), GSAP 3.12+, Lenis 1.x via lenis/react + single-RAF gsap.ticker pattern
- (assumption) [ASSUMPTION] resume.pdf generation mechanism deferred to build (content single-source is fixed; tool choice md-to-pdf/pandoc/hand-crafted is seed)
- (assumption) [ASSUMPTION] package manager pnpm; exact Next/Tailwind patch versions pinned at scaffold
- (event) spine drafted on fast path; reviewer gate next
- (decision) review-fix AD-2: reduced-motion gate is one policy in two coordinated halves -- global CSS prefers-reduced-motion:reduce kills glitch keyframes outright + gsap.matchMedia() kills JS tweens/Lenis smoothing; mobile 30% reduction is a single amplitude token; gsap.ticker passes seconds (lenis raf time*1000), lagSmoothing(0), lenis.on scroll -> ScrollTrigger.update
- (decision) review-fix AD-3: EmailOctopus API v2 (legacy v1.6 not for new builds); status SUBSCRIBED to avoid double-opt-in PENDING silently dropping inbox notification; contacts metric = list member count + tag; shared zod schema in lib/contact/schema.ts used by client+server; client judges by body.ok; honeypot hidden field website -> fake ok, no delivery
- (decision) review-fix AD-6: Vercel Web Analytics is cookieless (day-scoped request hash) -- consent gate retained as conservative tracking-privacy posture, not cookie-based; app-code <Analytics/> injection only, dashboard auto-injection forbidden so the gate is enforceable; consent key glitch-guy0:consent = accepted|declined, CookieBanner sole owner
- (decision) review-fix AD-8 (new): section anchor contract -- fixed ids #hero..#contact, native anchors + Lenis scrollTo, hash in URL for back-button, ScrollTrigger.refresh on hashchange
- (event) reviewer gate complete: 3 subagent lenses, fixes applied; PRD premise 'Web Analytics uses cookies' flagged as stale to correct upstream
- (event) spine finalized: status=final, reviewer fixes applied, lint clean
